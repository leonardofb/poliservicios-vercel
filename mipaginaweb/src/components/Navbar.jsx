import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar">
            {/* Aquí va tu código HTML de la barra de navegación */}
        </nav>
    );
};

export default Navbar;